# courier-data-simulator

Docker user must have necerray permission for testing
Will the pipeline be exactly once? if not check kafka integration tests (has size may needs to be changed to has size or greater in some places).
